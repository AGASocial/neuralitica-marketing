# SpecFounder — REFERENCE ONLY

The directories `.specfounder/` and `specfounder-v2/` are **not** product authority.

- Do not override `SPEC.md`, `CONTEXT.md`, `plan/USER_STORIES.md`, or ADRs with SpecFounder drafts.
- Do not auto-boot SpecFounder into `$desarrollar` or master-orchestrator.
- Useful for historical ADR drafts and methodology reference only.

Canonical ADRs for this product should live under `docs/adr/` when promoted; until then treat `.specfounder/adr/*` as drafts unless SPEC explicitly points to them.
